import 'package:flutter/material.dart';

class AppColors {
  static const background = Color.fromRGBO(245, 247, 250, 1); // soft off-white
  static const primary = Color.fromRGBO(47, 128, 237, 1); // calm blue
  static const secondary = Color.fromRGBO(86, 204, 242, 1); // light blue
  static const textDark = Color.fromRGBO(31, 41, 55, 1); // dark text
  static const textLight = Color.fromRGBO(107, 114, 128, 1); // subtitle text
  static const card = Color.fromRGBO(255, 255, 255, 1); // white

  static const Color transparentColor = Colors.transparent;
}
